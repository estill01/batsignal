<%= simple_form_for [@user, @agenda], :html => {:class => 'form-horizontal'} do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :private, :as => :check_boxes, :collection => ["Yes"] %>
    <%= f.button :submit %>
<% end %>
