<%= simple_form_for @user, :multipart => true, :html => {:class => 'form-vertical'} do |f| %>
    <%= f.input :username%>
    <%= f.input :password %>
    <%= f.input :email %>
    <%= f.input :phone_number, :hint => "We hate spam and won't do it." %>
    <%= f.button :submit %> 
<% end %>
